document.addEventListener('DOMContentLoaded',()=>{const fmt=n=>new Intl.NumberFormat('es-ES',{style:'currency',currency:'EUR',maximumFractionDigits:2}).format(Number.isFinite(n)?n:0);const num=(f,n)=>parseFloat(new FormData(f).get(n))||0;const output=(f,h)=>{const r=f.parentElement.querySelector('#result');if(r){r.innerHTML=h;r.classList.add('show')}};document.querySelectorAll('.calculator').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const t=form.dataset.calc;if(t==='finiquito'){const s=num(form,'salary'),d=num(form,'days'),v=num(form,'vacdays'),ex=num(form,'extra'),day=s/30,a=day*d,b=day*v;output(form,`<div class="big">${fmt(a+b+ex)}</div><div class="result-grid"><div><small>Días trabajados</small>${fmt(a)}</div><div><small>Vacaciones</small>${fmt(b)}</div><div><small>Pagas extra</small>${fmt(ex)}</div></div>`)}if(t==='despido'){const s=num(form,'salary'),y=num(form,'years'),days=num(form,'type'),day=s/30;output(form,`<div class="big">${fmt(day*days*y)}</div><div class="result-grid"><div><small>Salario día</small>${fmt(day)}</div><div><small>Días/año</small>${days}</div><div><small>Antigüedad</small>${y} años</div></div>`)}if(t==='paro'){const base=num(form,'base'),days=num(form,'days'),child=num(form,'children');let m=0;if(days>=360)m=4;if(days>=540)m=6;if(days>=720)m=8;if(days>=900)m=10;if(days>=1080)m=12;if(days>=1260)m=14;if(days>=1440)m=16;if(days>=1620)m=18;if(days>=1800)m=20;if(days>=1980)m=22;if(days>=2160)m=24;output(form,`<div class="big">${m} meses</div><div class="result-grid"><div><small>Primeros 180 días</small>${fmt(base*.70)}</div><div><small>Después</small>${fmt(base*.60)}</div><div><small>Hijos</small>${child}</div></div><p class="microcopy">No aplica topes mínimos/máximos exactos. Revísalo con SEPE.</p>`)}if(t==='vacaciones'){const fd=new FormData(form),a=new Date(fd.get('start')),b=new Date(fd.get('end')),annual=num(form,'annual'),used=num(form,'used');let worked=0;if(!isNaN(a)&&!isNaN(b)&&b>=a)worked=Math.ceil((b-a)/(86400000))+1;const gen=worked*annual/365,p=Math.max(0,gen-used);output(form,`<div class="big">${p.toFixed(1)} días</div><div class="result-grid"><div><small>Días trabajados</small>${worked}</div><div><small>Generados</small>${gen.toFixed(1)}</div><div><small>Disfrutados</small>${used}</div></div>`)}if(t==='nomina'){const g=num(form,'gross'),p=num(form,'pays')||12,ir=num(form,'irpf')/100,ss=num(form,'ss')/100,net=g-(g*ir)-(g*ss);output(form,`<div class="big">${fmt(net/p)}</div><div class="result-grid"><div><small>Neto anual</small>${fmt(net)}</div><div><small>IRPF estimado</small>${fmt(g*ir)}</div><div><small>Seg. Social</small>${fmt(g*ss)}</div></div>`)}if(t==='reduccion'){const s=num(form,'salary'),c=num(form,'current'),n=num(form,'new'),ns=c?s*(n/c):0;output(form,`<div class="big">${fmt(ns)}</div><div class="result-grid"><div><small>Salario actual</small>${fmt(s)}</div><div><small>Pérdida mensual</small>${fmt(s-ns)}</div><div><small>Nueva jornada</small>${n}%</div></div>`)}if(t==='coste'){const g=num(form,'gross'),co=num(form,'company')/100,o=num(form,'other'),cost=g+(g*co)+o;output(form,`<div class="big">${fmt(cost)}</div><div class="result-grid"><div><small>Bruto anual</small>${fmt(g)}</div><div><small>Cotización empresa</small>${fmt(g*co)}</div><div><small>Otros costes</small>${fmt(o)}</div></div>`)}if(t==='horas'){const s=num(form,'salary'),w=num(form,'weekly'),h=num(form,'hours'),b=num(form,'bonus')/100,mh=w*52/12,hour=mh?s/mh:0;output(form,`<div class="big">${fmt(hour*(1+b)*h)}</div><div class="result-grid"><div><small>Valor hora base</small>${fmt(hour)}</div><div><small>Horas extra</small>${h}</div><div><small>Recargo</small>${(b*100).toFixed(0)}%</div></div>`)}if(t==='baja'){const s=num(form,'salary'),d=num(form,'days'),v=num(form,'vacdays'),req=num(form,'required'),given=num(form,'given'),ex=num(form,'extra'),day=s/30,a=day*d,b=day*v,disc=day*Math.max(0,req-given);output(form,`<div class="big">${fmt(a+b+ex-disc)}</div><div class="result-grid"><div><small>Salario pendiente</small>${fmt(a)}</div><div><small>Vacaciones</small>${fmt(b)}</div><div><small>Descuento preaviso</small>${fmt(disc)}</div></div><p class="microcopy">Estimación bruta. La baja voluntaria normalmente no genera indemnización.</p>`)}if(t==='salariohora'){const sal=num(form,'salary'),period=new FormData(form).get('period'),w=num(form,'weekly'),p=num(form,'pays')||12,annual=period==='annual'?sal:sal*p,hours=w*52,hour=hours?annual/hours:0;output(form,`<div class="big">${fmt(hour)} / hora</div><div class="result-grid"><div><small>Salario anual</small>${fmt(annual)}</div><div><small>Salario mensual medio</small>${fmt(annual/12)}</div><div><small>Horas/año</small>${hours.toFixed(0)}</div></div>`)}if(t==='pagasextra'){const sal=num(form,'salary'),extras=num(form,'extras'),months=num(form,'months'),pro=new FormData(form).get('prorated'),prop=sal*Math.min(months,12)/12,month=(sal*extras)/12;output(form,`<div class="big">${fmt(pro==='yes'?month:prop)}</div><div class="result-grid"><div><small>Paga completa</small>${fmt(sal)}</div><div><small>Proporcional</small>${fmt(prop)}</div><div><small>Prorrata mensual</small>${fmt(month)}</div></div>`)}if(t==='antiguedad'){const fd=new FormData(form),start=new Date(fd.get('start')),end=new Date(fd.get('end'));if(isNaN(start)||isNaN(end)||end<start){output(form,`<div class="big">Fechas no válidas</div><p class="microcopy">Comprueba que la fecha de fin sea posterior a la fecha de inicio.</p>`)}else{let y=end.getFullYear()-start.getFullYear(),m=end.getMonth()-start.getMonth(),d=end.getDate()-start.getDate();if(d<0){m--;d+=new Date(end.getFullYear(),end.getMonth(),0).getDate()}if(m<0){y--;m+=12}const total=Math.ceil((end-start)/86400000)+1;output(form,`<div class="big">${y} años, ${m} meses y ${d} días</div><div class="result-grid"><div><small>Años</small>${y}</div><div><small>Meses</small>${m}</div><div><small>Días totales</small>${total}</div></div>`)}}}));const toggle=document.querySelector('.menu-toggle'),nav=document.querySelector('#site-nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))})}const banner=document.getElementById('cookie-banner');if(banner){const hide=()=>{banner.setAttribute('hidden','');banner.classList.add('is-hidden');banner.style.display='none'};const show=()=>{banner.removeAttribute('hidden');banner.classList.remove('is-hidden');banner.style.display=''};const saved=localStorage.getItem('cl_consent');saved==='accepted'||saved==='rejected'?hide():show();document.querySelectorAll('[data-consent]').forEach(btn=>btn.addEventListener('click',()=>{const ok=btn.dataset.consent==='accept';localStorage.setItem('cl_consent',ok?'accepted':'rejected');if(window.gtag){gtag('consent','update',{ad_storage:ok?'granted':'denied',analytics_storage:ok?'granted':'denied',ad_user_data:ok?'granted':'denied',ad_personalization:ok?'granted':'denied'})}hide()}))}});